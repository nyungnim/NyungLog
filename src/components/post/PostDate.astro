---
import { formatDate } from "../../utils/tools";
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-sm text-gray-600 dark:text-gray-400">
  {formatDate(date)}
</time>
